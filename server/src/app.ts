/**
 * bchat — Application Entry Point
 *
 * Thin wrapper that bootstraps the Express server.
 * Use this as the entry point for production and development:
 *   npm run dev   → tsx watch src/app.ts
 *   npm start     → node dist/app.js
 */

import './server';
